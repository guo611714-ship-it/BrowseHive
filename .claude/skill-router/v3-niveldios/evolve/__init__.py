"""Skill Router V3 — Auto-evolución (Agent E).

Detecta ghosts, cold clusters, gap queries, false positives y gate friction
desde el audit log JSONL (Agent B) y genera reports semanales con propuestas.
"""

__version__ = "1.0.0"
