name = "github-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 路由配置在 Cloudflare Dashboard 中设置
# 需要添加路由: your-subdomain.example.com/*

# 环境变量（可选）
# [vars]
# UPSTREAM = "https://github.com"

# 如果需要使用自定义域名，请配置以下
# [env.production]
# routes = ["your-domain.com/*"]
